const add = (first, second) => first + second;

const multiply = (first, second) => first * second;

const divideTheFirstumberByTheSecondNumber = (first, second) => {
  return first / second;
};

export { add, multiply, divideTheFirstumberByTheSecondNumber };
